# MapIA Active Snapshot

This folder contains the former active MapIA-derived implementation snapshot.

It is preserved for historical and engineering reference only. It is not the
canonical MYCELIA runtime, and it must not be used as the active implementation
foundation unless a future task explicitly instructs Codex to do so after
architecture review.

Canonical MYCELIA architecture lives in `docs/mycelia/`.
